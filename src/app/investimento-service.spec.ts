import { InvestimentoService } from './investimento-service';

describe('InvestimentoService', () => {
  it('should create an instance', () => {
    expect(new InvestimentoService()).toBeTruthy();
  });
});
